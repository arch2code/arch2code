include:
    - ip.yaml

parameters:
    ip:
        variant0:
            IP_DATA_WIDTH: 8
            IP_MEM_DEPTH: 16
            IP_NONCONST_DEPTH: 24
        # variant1 is declared where it is instantiated, in bridge/yaml/ipBridge.yaml.
