# subB-owned leaf context. Drags subB's vendored shared copy via include:.
include:
    - ../shared/yaml/shared_types.yaml

blocks:
    subBLeafBlock:
        desc: "subB-owned leaf block"
        hasMdl: true
        hasRtl: false
