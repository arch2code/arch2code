# Create-once scaffold, hand-written so the copy under test resolves through
# FIXTURE_ROOT instead of the surrounding git tree.
include ../include/make/shared.mk

include $(A2C_ROOT)/include/make/a2c-rtl.mk
