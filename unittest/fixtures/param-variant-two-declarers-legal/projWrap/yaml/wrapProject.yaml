# projWrap PROJECT file. References projIp so the leaf stays owned by its
# author, and owns wrapTop.yaml, which binds its own v0 of the leaf.
projectName: projWrap

projectFiles:
    - ../../projIp/yaml/ipProject.yaml
    - wrapTop.yaml

dirs:
    root: ..

fileGeneration:
    template: none
