include:
    - ../../childUTop/yaml/childUTopArch.yaml
    - ../../childNamed/yaml/childNamedArch.yaml

blocks:
    asmTop: { desc: "Assembler top" }
    rogue:  { desc: "Root-owned block placed inside itself" }

instances:
    uAsm:    { container: asmTop, instanceType: asmTop }
    uChildU: { container: asmTop, instanceType: cuTop }
    uChildN: { container: asmTop, instanceType: cnTop }
    rogue:   { container: rogue,  instanceType: rogue }
