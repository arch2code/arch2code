# Shared types authored in the shared project (standalone vendor master copy).
types:
    sharedPixel: { width: 8, desc: "shared pixel type" }
    sharedCoord: { width: 12, desc: "shared coordinate type" }
