# childA PROJECT file. Reached through the root's projectFiles: slot and
# carrying the project sentinel set, so projectName childAProj owns this context
# and its whole transitive closure (childATop.yaml), including the address group
# declared there.
projectName: childAProj

projectFiles:
    - childATop.yaml

dirs:
    root: ..

fileGeneration:
    template: none
    # This project's own fileMap names its firmware headers, in the composed
    # build as on its own.
    fileMap:
        includeFW: { name: "IncludesFW", ext: {hdr: "h", src: "cpp"}, cond: {smartInclude: true}, mode: context, basePath: fwInc, langDomain: fw, desc: "yaml based fw include file" }
